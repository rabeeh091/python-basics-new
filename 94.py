#The tuple() cunstructor

#it is also possible to use the tuple() constructor to make a tuple.

#using the tuple() method to make a tuple?

thistuple = tuple(["apple","banana","cherry"])

print(thistuple)